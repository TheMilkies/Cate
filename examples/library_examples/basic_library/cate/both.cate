Library greeter(static);
greeter.flags = "-O3";
greeter.out = "out/libgreeter.a";
greeter.files = {"src/libgreeter/greeter.c"};
greeter.includes = {"include"};
greeter.build();

//make dynamic now
greeter.type = dynamic;
greeter.out = "out/libgreeter.so";
greeter.build();